# IMP-039 제출·서명 공격 회귀 검증 기록

| 항목 | 결과 |
|---|---|
| 검증일 | 2026-07-23 |
| 대상 | Online credential 제출과 Offline signed/user 제출 경계 |
| 시험 자료 | 개인정보가 없는 합성 Package·합성 인증서·메모리 저장소 |
| 공격 수 | 28개 — Online 13개, Offline 15개 |
| 차단 | 28개 |
| 통과 | 0개 |
| 공격 자료 저장·정규화·판정·Finding | 모두 0회 |
| 공식 Finding | 생성 안 함 |
| 운영 endpoint | 비활성 |
| 결과 | PASS |

## 1. 구현 범위

- Package·descriptor·Manifest·hash 변조
- 만료 credential·Manifest·offline signature envelope
- 사용된 credential과 nonce 재전송
- Offline signed 제출 후 user profile로 변경한 재전송
- 다른 Organization·Asset·Job·Manifest·Content-Type
- Manifest 서명 실패
- Offline signature 변조, 잘못된 EKU, 다른 Asset SAN, 미신뢰 root
- 폐기 인증서와 오래되거나 확인할 수 없는 폐기 상태
- 로그인·CSRF·Asset 권한이 없는 사용자 제출

모든 공격은 현재 제출 서비스의 공개 오류 코드까지 확인했다. 예상하지 않은 내부 예외도 성공으로 간주하지 않지만, 이번 결과에는 예상하지 않은 오류가 없었다.

## 2. 처리 경계

공격 입력이 접수 검증을 통과한 경우에만 후속 처리 계측값이 증가하도록 구성했다.

| 후속 처리 | 호출 |
|---|---:|
| 공격 제출 수락 | 0 |
| 정본 저장 | 0 |
| Evidence 정규화 | 0 |
| Rule 실행 | 0 |
| Finding 저장 | 0 |
| 공식 Finding 생성 | 0 |

Package 검증 실패를 Control `ERROR` Finding으로 바꾸지 않았다. 유효한 baseline 제출은 replay 상태를 만들기 위한 시험 준비에만 사용했고 공격 수락 수치에 포함하지 않았다.

## 3. 안전한 오류·보고

- 보고서에는 stable public code와 쉬운 한국어 안내만 포함한다.
- bearer credential, 개인키, 인증서 본문, 원시 Evidence와 임시 경로를 포함하지 않는다.
- 실제 예외 메시지와 stack trace를 Web/API 응답에 포함하지 않는다.
- 정적 UI 보고서는 새 공격 실행 결과와 자동시험에서 전체 object equality로 대조한다.
- 개발 화면과 API는 `SECAI_DEV_DEMO_ENABLED=true`에서만 열리고 그 외에는 404다.

## 4. Web·API 확인

- UI: `http://127.0.0.1:18480/ui/submission-attack`
- API: `http://127.0.0.1:18480/api/v1/demo/submission-attack`
- UI HTTP: 200
- API 상태: PASS
- API 결과: 공격 28개·차단 28개·통과 0개·Finding write 0

## 5. 자동검증

```text
powershell -NoProfile -ExecutionPolicy Bypass -File .\tools\verify-imp039-submission-attack.ps1
powershell -NoProfile -ExecutionPolicy Bypass -File .\tools\dev.ps1 -Action All
```

- IMP-039 집중시험: 7개 PASS
- 전체 Pytest: 347 PASS
- JSON Schema: 9 schemas·16 examples PASS
- Ruff: PASS
- mypy strict: 172 source files PASS
- 기존 Starlette deprecation warning 1건만 유지

## 6. Docker 동기화

source와 실행 컨테이너가 달라지지 않도록 API·worker·scheduler를 재빌드하고 재생성했다.

| Image | ID |
|---|---|
| `sec-ai-mvp/audit-api:0.1.0` | `sha256:ce2fae14e931f0dd2afa0dd5c7b94aa92cccbef0c07fd34b523c0e09a938c552` |
| `sec-ai-mvp/audit-worker:0.1.0` | `sha256:1809db9b08f8a5ea2d8706ee5037759b9921e2c4426fa4bb2d80a8ab05aaee3b` |
| `sec-ai-mvp/audit-scheduler:0.1.0` | `sha256:9f47c0bf5e5183c18fca2543e6175d4d02cbb4c2d81cd4fb6837259da7b57410` |

API health는 `ok`, readiness의 PostgreSQL·Redis·AIStor·ClamAV는 모두 true다.

## 7. 남은 경계

- PostgreSQL 기반 전역 제출 replay 저장소와 운영 upload endpoint는 아직 없다.
- 실제 조직 인증서·CRL/OCSP·SmartScreen·clean Windows VM 검증은 단계 J에서 수행한다.
- 이번 PASS는 합성 개발 공격시험 결과이며 운영 배포 승인이 아니다.
- 다음 작업은 `IMP-040 — 사용자용 제품 골격·1클릭 일반 점검`이다.

