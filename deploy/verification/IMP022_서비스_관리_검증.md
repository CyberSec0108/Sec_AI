# IMP-022 서비스와 사용 환경 설정 검증

## 1. 결과

`IMP-022`의 PC-04~09 확대를 완료했다. 새 판정 코드는 PC-04·05·06·08·09를 담당하고, PC-07은 기존 승인 규칙을 그대로 사용한다. 모든 결과는 합성 시험 자료이며 실제 PC 증적이나 운영 공식 판정이 아니다.

| 항목 | 결과 |
|---|---|
| 최신 개발 Pack | `audit_packs/kisa_2026_pc/src/pack-0.3.0.json` |
| Pack 상태 | `DRAFT` |
| Pack content SHA-256 | `6d7600e959e18885d2caf7a1c483fa721c5d0f0e5e9acb5e5b9c73667883ca38` |
| 신규 합성 사례 | 20개 |
| 기존 PC-07 사례 | 17개, 변경 없음 |
| 신규 상태 범위 | `PASS`, `FAIL`, `ERROR`, `REVIEW`, `N/A` |
| Pytest | 174개 통과 |
| Ruff | 통과 |
| mypy strict | 82개 source file 통과 |

## 2. 실제 컨테이너 확인

2026-07-23에 `tools/core.ps1 -Action Up`으로 현재 소스를 다시 빌드했다.

- `sec-ai-mvp/*:0.1.0` 이름의 8개 서비스가 모두 `healthy`
- API image digest: `sha256:2e342b22d18490852b98e6190d9c9b0dc33ec56e46ffa8d15e63d56d9ed0e7ba`
- `GET /ui/service-management`: HTTP 200, 실행 버튼 20개
- `GET /api/v1/demo/service-management`: 결과 20개
- API 상태: `PASS`, `FAIL`, `ERROR`, `REVIEW`, `N/A`
- `GET /health/ready`: PostgreSQL·Redis·AIStor·ClamAV 모두 `true`
- 화면 응답 헤더: `Cache-Control: no-store`, `X-Frame-Options: DENY`, `X-Content-Type-Options: nosniff`

인앱 브라우저가 이 세션에 연결되지 않아 시각적 클릭·스크린샷 검증은 실행하지 못했다. 실제 Gateway를 통한 HTML·JSON·보안 헤더 검증과 FastAPI TestClient UI 상호작용 시험으로 보완했으며, 이를 시각 검증 완료로 표기하지 않는다.

## 3. 원문 확인

원문은 `data/주요정보통신기반시설 기술적 취약점 분석·평가 방법 상세가이드.pdf`를 사용했다.

- 문서 SHA-256: `44fe393981b244147be6af7423d99dc15633c089fad0bcb296cbe2371dde812d`
- PC-04: 561~565쪽
- PC-05: 566~569쪽
- PC-06: 570쪽
- PC-07: 571~572쪽
- PC-08: 573~574쪽
- PC-09: 575~576쪽

## 4. 잘못된 자동 판정을 막는 경계

- PC-04 공유 용도와 승인 권한 기준이 없으면 `REVIEW`다.
- PC-05 서비스가 업무상 필요한지 정한 조직 목록이 없으면 `REVIEW`다.
- PC-06 메신저 허용·금지 목록이 없거나 제품 식별 신뢰도가 낮으면 `REVIEW`다.
- 수집 실패·권한 부족은 보안 위반인 `FAIL`이 아니라 `ERROR`다.
- PC-08은 Recovery·진단·가상화 항목을 부팅 가능한 OS 수에서 제외한다.
- WSL과 Hyper-V Guest는 호스트의 멀티부팅 OS로 계산하지 않는다.
- PC-09는 IE 데스크톱·IE 모드·WinINet을 모두 쓰지 않는다는 조직 범위 확인이 있을 때만 `N/A`다.
- PC-09 기준을 Edge나 다른 브라우저 전체에 임의로 확대하지 않는다.

## 5. 재실행 파일

전체 표준 시험:

```powershell
powershell -NoProfile -ExecutionPolicy Bypass -File .\tools\dev.ps1 -Action All
```

IMP-022 집중시험:

```text
tests/contract/test_imp022_service_management_pack.py
tests/unit/test_imp022_service_management_rule.py
tests/unit/test_imp022_service_management_ui.py
tests/unit/test_imp021_ui_standardization.py
```

화면:

```text
http://localhost:18480/ui/service-management
```

## 6. 다음 단계 경계

다음은 `IMP-023` PC-10~11 패치·지원 수명 기준이다. 이 문서는 IMP-023 구현이나 실제 Windows 수집을 승인하지 않는다.
