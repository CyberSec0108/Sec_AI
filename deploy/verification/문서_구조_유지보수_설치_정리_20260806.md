# 문서 구조·유지보수·다른 PC 설치 정리 검증

| 항목 | 결과 |
|---|---|
| 기준일 | 2026-08-06 |
| 범위 | 폴더·파일 기능 카탈로그, 유지보수 가이드, ADR 최신화, 중복 문서 정리, 다른 PC 설치·이전 안내 |
| 제품 로직 | 변경 없음 |
| 인프라 정합화 | portable Core image 목록에 maintenance-worker·model-gateway·pgAdmin 추가, PostgreSQL 기반 image lock을 현재 pgvector Dockerfile과 일치시킴 |
| 운영 승격 | 없음 |

## 1. 추가·갱신 문서

- `docs/maintenance/프로젝트_구조_및_파일_기능_카탈로그.md`
  - Runtime·secret·cache를 제외한 source·설정·시험·문서를 폴더별로 분류한다.
  - 파일마다 기능, 주요 정의·계약, 유지보수 주의를 기록한다.
  - `tools/generate-repository-catalog.ps1`로 재생성한다.
- `docs/maintenance/유지보수_가이드.md`
  - 변경 유형별 담당 위치, 보안 경계, 검증, 장애 대응과 문서·ADR 관리 절차를 통합한다.
- `docs/guides/다른_PC_설치_및_이전_안내.md`
  - source-only와 offline image bundle, hash 검증, Secret·license·VM 분리, Core 시작·확인·문제 해결을 설명한다.
- `docs/adr/13`, `15`, `19`, `20`과 ADR index
  - Windows 기준선 완료, Linux·Profile·BGE 부분 구현, Switch·운영 Pack·로컬 vLLM·Agent/MCP 미완료 경계를 반영한다.
  - 특정 PC의 `E:` 절대경로를 프로젝트 상대경로 또는 현 저장소 미포함 초기 원본 표기로 정리한다.

## 2. 삭제 Manifest

| 삭제 경로 | 크기 | 삭제 전 SHA-256 | 사유 | 대체·복구 |
|---|---:|---|---|---|
| `명령` | 503 bytes | `EC520D55AE63791150C0492B529357CD303CF455E5376B769FCCA568713A351D` | `AGENTS.md`·`구현_현황.md`의 시작 규칙을 중복한 루트 임시 메모 | 아래 원문과 `AGENTS.md`·`구현_현황.md`로 복구 가능 |

삭제 전 원문은 다음과 같습니다. 이 기록은 감사·복구용이며 작업 지침 정본이 아닙니다.

```text
C:\Users\Hala\Desktop\Sec_AI 프로젝트 작업을 이어서 진행한다.

먼저 루트 AGENTS.md와 구현_현황.md를 읽고,
다음_I_J_단계_계획.md 및 대화_RAG_제품_계획.md를 확인한다.
다음 작업을 파악한다.


다음 작업을 시작하라고 할때 아래 사항을 준수한다.
기존 사용자 작업을 보존하고, 이동 묶음은 생성하지 않는다.
완료 후 전체 검증과 deploy/verification 기록,
구현_현황.md 갱신까지 수행한다.
```

`deploy/verification`의 과거 IMP·PRODUCT-AI 기록은 현재 구현과 문구가 다르더라도 완료 주장·공급망·시험의 감사 계보이므로 삭제하지 않았습니다. 계획·ADR·사용자 안내처럼 현재 정본 역할이 있는 문서는 삭제 대신 최신 상태와 미완료 Gate를 명시했습니다.

## 3. 검증 결과

- [x] 파일 카탈로그 생성기를 Windows PowerShell에서 2회 실행
  - 결과: 859 files·107 folders, 두 실행 SHA-256 `A1F3F2C7ED68C6B8EFB61C6FFBB8BA1F9B83066FA6A9BF4E1507E9CF874A246D` 일치
- [x] 생성 카탈로그 제외 경계
  - 결과: 실제 `.env`, `runtime`, `.runtime`, `downloads`, `tmp` file/section 0
- [x] 변경 PowerShell parser
  - 결과: `generate-repository-catalog.ps1`, `core.ps1`, `export-portable-bundle.ps1` 3/3 PASS
- [x] portable image 목록과 현재 Compose project image 비교
  - 결과: 로컬 vLLM 제외 현재 project image 12/12 일치, Dockerfile base 8/8 lock 일치
- [x] ADR index SHA-256과 실제 파일 hash 비교
  - 결과: 20/20 일치, ADR의 특정 PC `E:` 절대경로 0
- [x] Markdown 상대 링크 존재 검사
  - 결과: 148 files, broken link 0
- [x] `docker compose config`
  - 결과: PASS
- [x] 신규 문서 secret pattern 검사
  - 결과: private key·API key·Bearer token 패턴 0
- [x] 중복 루트 메모 삭제 확인
  - 결과: `명령` 없음, 삭제 전 hash·원문·대체 문서 기록 완료

## 4. 전체 표준 Gate 결과

`powershell -NoProfile -ExecutionPolicy Bypass -File .\tools\dev.ps1 -Action All`을 실행했으나 현재 작업 밖 기존 코드·시험 불일치로 전체 PASS에는 도달하지 못했습니다.

| Gate | 결과 | 이번 변경 관련성 |
|---|---|---|
| Compose config | PASS | portable image 정합화와 직접 관련 |
| Pytest | `707 passed, 11 failed, 1 warning` | 실패는 기존 DB table 기대 목록, UI 문구/단계, Schema catalog 기대 수 25↔실제 26으로 이번 문서·PowerShell 변경과 무관 |
| Schema example | FAIL 1 | `valid/finding_explanation_input.json`의 기존 `evidence_trace` 누락 |
| Ruff | FAIL 4 | 기존 Collector 3파일과 `tools/build-full-guide-artifacts.py` import 정렬 |
| mypy strict | FAIL 2 | 기존 `tests/unit/test_assessment_criteria_profiles.py` type 오류, 297 files 검사 |

실패 기대값을 약화하거나 범위 밖 source를 수정하지 않았습니다. 이번 변경의 직접 검증인 문서 링크·ADR hash·PowerShell parser·portable image/base lock·Compose config·secret 제외는 모두 통과했습니다.
