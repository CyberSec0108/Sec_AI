# 로그인 원클릭 보안 점검 브랜드 UI 수정 검증

| 항목 | 결과 |
|---|---|
| 검증일 | 2026-08-01 |
| 범위 | 로그인 화면 브랜드·문구 정리와 공통 브랜드 마크 통일 |
| 판정 | PASS |
| 다음 작업 | `IMP-055` 깨끗한 Windows 11 기준 VM 제품 회귀 |

## 1. 변경

- 문자 `S` 마크를 원클릭 점검 목표를 나타내는 방패·체크 SVG로 변경했다.
- 로그인·두 번째 인증·Launcher 연결·공통 상단 브랜드에서 같은 마크를 사용한다.
- 브랜드 설명 `PC 보안 점검`을 `원클릭 보안 점검`으로 바꿨다.
- 로그인 화면에서 다음 사용자 요청 문구를 삭제했다.
  - `내 계정으로 로그인`
  - PC 점검 결과와 조직 자료 분리 소개 문장
  - 개발용 두 번째 인증·향후 WebAuthn/OIDC 설명
  - 보안 프로필 표시
- `현재는 개발용 로그인입니다.` 안내와 사용자 이름·비밀번호·계속 기능은 유지했다.
- 인증·CSRF·MFA·session·권한 로직은 변경하지 않았다.

## 2. 검증

- 실패 우선 로그인 UI 회귀 1건 확인 후 구현
- 인증·제품 홈·최종 UI 집중 Pytest: 22 PASS
- 변경 테스트 Ruff: PASS
- 실제 `http://127.0.0.1:18480/auth/login`: HTTP 200
- 실제 응답의 새 SVG·`원클릭 보안 점검`·삭제 문구 부재 확인
- Headless Chrome 760×1100 렌더링에서 아이콘·입력 폼·안내 영역 깨짐 없음
- 전체 표준 Gate는 직전 566 PASS 기준을 유지하며 이번 저위험 마크업 변경에서는 반복하지 않았다.

## 3. Runtime

- API image/container: `sha256:1ef4eb6a24b010955b490defe4228d74fbea3032ad9a86950b39b001c9a79f30`
- API container: `healthy`
- image/container digest: 일치
- PostgreSQL·Redis·model-gateway·Gateway 재시작 없음
