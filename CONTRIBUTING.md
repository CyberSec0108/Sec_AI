# Sec_AI 기여 가이드 (Contributing Guide)

Sec_AI 오픈소스 프로젝트에 관심을 가져주셔서 감사합니다!
이 문서는 프로젝트에 코드를 기여(코드 수정, 버그 리포트, 기능 추가 등)하거나 파일을 수정하고 커밋할 때 지켜야 할 **표준 Git 워크플로우 및 커밋 컨벤션**을 안내합니다.

---

## 1. 커밋(Commit) 기본 원칙

Sec_AI 프로젝트는 **"단일 목적 단위의 커밋(Atomic Commit)"**을 지향합니다.
하나의 커밋에는 **'하나의 논리적인 변경 사항'**만 포함되어야 합니다. 예를 들어, 웹 UI 디자인을 수정하는 작업과 데이터베이스 스키마를 변경하는 작업은 별도의 커밋으로 분리해야 합니다.

### 올바른 커밋 워크플로우

#### Step 1: 수정된 파일 상태 확인
코드를 수정한 뒤, 무작정 업로드하기 전에 어떤 파일들이 변경되었는지 확인합니다.
```bash
git status
```

#### Step 2: 목적이 같은 파일만 Staging (장바구니 담기)
관련 있는 파일들만 콕 집어서 추가합니다.
```bash
git add src/security_audit/guides/public_guides.py
git add README.md
```
*(만약 모든 변경 사항이 하나의 목적을 가지고 있다면 `git add .`을 사용해도 무방합니다.)*

#### Step 3: 규격화된 메시지로 커밋 (Commit)
오픈소스 생태계의 표준인 **Conventional Commits (말머리 규칙)**을 사용하여 커밋 메시지를 작성합니다.
```bash
git commit -m "docs: 가이드라인 PDF 파싱 라이브러리 교체 및 라이선스 갱신"
```

#### Step 4: 깃허브로 푸시 (Push)
```bash
git push
```

---

## 2. 커밋 메시지 컨벤션 (말머리 규칙)

커밋 메시지는 다른 기여자들이 변경 사항을 한눈에 파악할 수 있도록 반드시 아래의 말머리(Prefix) 중 하나로 시작해야 합니다.

| 말머리 (Type) | 사용 목적 및 상황 | 예시 |
| :--- | :--- | :--- |
| **`feat:`** | 새로운 기능이 추가되었을 때 | `feat: 웹 UI 기반 점검 결과 PDF 내보내기 기능 추가` |
| **`fix:`** | 버그나 오류를 수정했을 때 | `fix: 윈도우 점검 수집기에서 발생하는 인코딩 깨짐 현상 수정` |
| **`docs:`** | 문서(README, 주석, 가이드)만 수정했을 때 | `docs: 라이선스 정보 업데이트 및 CONTRIBUTING.md 추가` |
| **`chore:`** | 코드 로직 변경 없이 잡다한 설정, 패키지(requirements) 변경 시 | `chore: 안 쓰는 라이브러리(PyMuPDF) 삭제 및 버전업` |
| **`refactor:`** | 기능의 변화 없이 코드 구조나 로직을 더 깔끔하게 개선했을 때 | `refactor: 데이터베이스 연결 함수 구조화 및 모듈 분리` |
| **`style:`** | 코드 포매팅, 세미콜론 누락 수정 등 (로직 변화 없음) | `style: Ruff 린터 규칙에 맞게 띄어쓰기 교정` |
| **`test:`** | 테스트 코드를 추가하거나 수정했을 때 | `test: 신규 스위치 판정 로직에 대한 단위 테스트 추가` |

### 커밋 메시지 작성 팁
- 첫 줄은 **명령문/요약 형태**로 작성합니다. (예: `fix: 해결함` 보다는 `fix: OO 오류 수정`)
- 내용이 복잡하다면 첫 줄(요약)을 적고, 한 줄을 띄운 뒤에 상세한 이유를 적어주세요.
```text
refactor: 인증 모듈 로직 리팩토링

- 하드코딩된 토큰 만료 시간을 환경 변수(.env)에서 불러오도록 변경
- 불필요한 import 제거
```

---

## 3. Pull Request (PR) 및 브랜치 전략

- 직접 `main` 브랜치에 푸시하는 대신, 새로운 기능을 개발할 때는 기능별 브랜치를 생성해주세요. (예: `git checkout -b feature/login`)
- 작업이 완료되면 깃허브에서 **Pull Request (PR)**를 생성하여 코드 리뷰를 요청해 주시기 바랍니다.

다시 한번 Sec_AI 프로젝트와 함께해 주셔서 감사합니다! 🚀
